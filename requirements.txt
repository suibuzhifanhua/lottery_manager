# 双色球历史数据管理系统 依赖清单
# 安装命令（使用阿里云镜像）：
#   pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/

# ── 界面 ──────────────────────────────────────────────────────
tkcalendar>=1.6.1          # 日期选择控件

# ── 数据处理 ──────────────────────────────────────────────────
numpy>=1.24.0
scipy>=1.10.0
openpyxl>=3.1.0            # Excel 导出
requests>=2.28.0           # 历史数据抓取

# ── 机器学习 ──────────────────────────────────────────────────
scikit-learn>=1.3.0
joblib>=1.3.0
xgboost>=1.7.0

# ── 深度学习（CPU 版 PyTorch，体积更小） ──────────────────────
# Windows / macOS / Linux 通用 CPU wheel：
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

# ── 可视化（可选，gen_chart.py 使用） ────────────────────────
matplotlib>=3.7.0

# ── OCR 图片识别 ──────────────────────────────────────────────
Pillow>=9.0.0              # 图像处理
cnocr>=2.3.0               # 轻量级中文OCR（基于PyTorch）

# 安装命令（阿里云镜像）：
#   pip install cnocr -i https://mirrors.aliyun.com/pypi/simple/

# ── 打包工具 ──────────────────────────────────────────────────
pyinstaller>=6.0.0
